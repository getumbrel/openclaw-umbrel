services:
  openclaw:
    build: .
    image: openclaw:latest
    container_name: openclaw
    restart: unless-stopped
    init: true
    ports:
      - "18789:18789"
    volumes:
      - $PWD/data:/data
